{
  "name": "@neezer/cfg",
  "private": false,
  "version": "0.0.0-semantically-released",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.2.0",
    "toml": "^2.3.3"
  },
  "bin": "./bin/cfg.js",
  "files": [
    "bin",
    "dist",
    "package.json"
  ],
  "devDependencies": {
    "@commitlint/config-conventional": "^7.5.0",
    "@types/dotenv": "^6.1.0",
    "@types/fs-extra": "^5.0.4",
    "@types/node": "^10.12.14",
    "@types/tape": "^4.2.32",
    "fs-extra": "^7.0.1",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.2",
    "semantic-release": "^15.13.1",
    "tape": "^4.9.1",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf dist",
    "test": "tape --require ts-node/register 'test/**/*.test.ts'",
    "prepublishOnly": "yarn build",
    "postinstall": "bin/cfg.js $INIT_CWD"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
